{
  "name": "htoh-unified",
  "version": "1.0.0",
  "private": true,
  "description": "HomeToHome Unified Platform - React Dashboard with Vertex AI Agent Engine",
  "scripts": {
    "dev": "npm run dev --workspace=frontend",
    "dev:all": "powershell -ExecutionPolicy Bypass -File ./scripts/dev.ps1",
    "build": "npm run build --workspace=frontend",
    "preview": "npm run preview --workspace=frontend",
    "backend:dev": "cd backend && python -m uvicorn app.main:app --reload --port 8000",
    "docker:up": "docker-compose up -d",
    "docker:down": "docker-compose down"
  },
  "workspaces": [
    "frontend"
  ],
  "keywords": [
    "react",
    "vertex-ai",
    "firebase",
    "real-estate",
    "ethics-advisor"
  ],
  "author": "HomeToHome Team",
  "license": "MIT",
  "dependencies": {
    "@google/gemini-cli-core": "^0.33.1"
  }
}
